/* ============================================================
   Dream — Supabase config for the friends leaderboard.
   Fill these in with YOUR free Supabase project's values, then
   reload the app. Until you do, the rest of Dream works fine and
   the League tab shows setup instructions.

   Where to find these (takes ~10 min, free, no credit card):
     1. Create a project at https://supabase.com
     2. In the dashboard: Project Settings → Data API (or API)
     3. Copy the "Project URL" and the "anon public" key below
     4. Run the SQL from README.md in the Supabase SQL Editor

   These two values are SAFE to share/commit — the anon key is a
   public client key. Real protection comes from your table rules.
   ============================================================ */
window.DREAM_SUPABASE = {
  url:     'https://tnvciyxmcbymwfopgedd.supabase.co',
  anonKey: 'sb_publishable_SpuAFl395sbqiWdnP1B4BA_IDLmG4kY'  // publishable (public client) key
};
